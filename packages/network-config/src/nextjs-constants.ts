/**
 * A representative sample is enough for cache warming. The complete prerender
 * table stays in its sidecar so it cannot expand gateway configuration by
 * thousands of paths.
 */
export const MAXIMUM_ENTRY_PAGE_PATHS = 50;
