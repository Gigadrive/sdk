---
'@gigadrive/sdk': minor
---

Add organization management and product entitlement APIs to the SDK.

`client.organizations` now supports `get` and `create`, plus nested
`members.list` and `products` helpers for listing product access, checking
entitlements, and activating organization product plans.
